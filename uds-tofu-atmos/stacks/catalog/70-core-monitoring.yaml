
components:
  terraform:
    core-monitoring:
      settings:
        providers:
          - "uds"
        depends_on:
          1:
            component: "core-base"
      vars:
        package_requirements:
          source: "oci://ghcr.io/defenseunicorns/packages/uds/core-monitoring:{{.vars.uds_version}}"
          package_vars: {}
