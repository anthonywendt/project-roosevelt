# vars:
#   stage: "roosevelt"

# Environment level sensitive overrides for this stack
components:
  terraform:
    podinfo:
      vars:
        package_overrides:
          - component_name: "podinfo"
            chart_name: "podinfo"
            values:
              # - path: "ui.message"
              #   value: "This is a sensitive message!!"
              - path: "replicaCount"
                value: 3
            sensitive_values:
              - path: "ui.message"
                value: "This is a sensitive message!!"
            #   - path: "replicaCount"
            #     value: 3
