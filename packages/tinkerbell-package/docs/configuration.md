# Configuration

Tinkerbell in this package is configured through [Tinkerbell UDS package](#UDS_PACKAGE_REPO#) as well as a UDS configuration chart that supports the following:

## Additional Configuration Info Follows
